# server/ is NO LONGER excluded — api/index.js require()s it, so the
# Express app now ships as a Vercel serverless function. Excluding it
# would build a frontend whose every /api call 404s.

# Dev/build scratch
scratch/
*.py

# Local tooling
.claude/
node_modules/

# Tests and docs never need to reach a deployment
server/tests/
docs/

# Scaffold template src files — these are WebContainer templates with their
# own package.json/tsconfig; they aren't compiled by Vercel and their
# react/vite imports aren't installed at the repo root, causing spurious
# TS2307 warnings in every Vercel build log.
server/lib/codeagent/scaffold/src/
server/lib/codeagent/scaffold/vite.config.ts
